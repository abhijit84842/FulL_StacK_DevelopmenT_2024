<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About Page</title>
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <h1>This is About Page</h1>
      <h2 class=" text-blue-500 font-bold">Multiply two number <%=2+5 %></h2>
      <div class="form p-10 text-black >
        <form action="">
          <input class="block w-full h-12 outline-none bg-tansparent rounded-md p-5" type="text" placeholder="enter title.." />
          <textarea class="block w-full mt-5 rounded-lg p-5" placeholder="write somethings"></textarea>
          <button class=" mt-2 p-1 bg-blue-500 rounded-md" type="submit">Submit</button>
        </form>
      </div>
      <div class="tasks ml-10 mt-10 flex gap-5 p-10 ">

      <!-- ejs apply -->
      <% if(files.length > 0){ %>
        <% files.forEach(function(val){ %>
          <div class="task  w-72 rounded-md bg-zinc-500 p-3">
            <h2>Some task here..</h2>
            <a class="text-blue-800" href="">See More....</a>
          </div>

          <% }) %>

        <%  } else{ %>
          <h2 class="text-slate-50"> No Task yet....</h2>
        <% } %>
      </div>
    </div>

    <!-- <script src="./javascript/script.js"></script> -->
  </body>
</html>
